# Matched by type, not by device id, because there are five keyboards here and ids change on replug.
# Alt+Shift is built into the keymap rather than bound as a key, so it still works on the lock screen.
input type:keyboard {
    xkb_layout  "us,ru"
    xkb_options "grp:alt_shift_toggle"
}
