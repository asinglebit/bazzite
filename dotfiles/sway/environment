# Plain assignments here are exported into sway's environment; driver variables go in /etc/sway/environment.

# Qt has no theme for "sway" and would go light, so send it to the portal like GTK4 and Firefox.
# Not gtk3, which flattens text inputs into the dialog background.
QT_QPA_PLATFORMTHEME=xdgdesktopportal
