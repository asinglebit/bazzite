# Deliberately empty: same basename means this replaces /usr/share/sway/config.d/95-autostart-policykit-agent.conf.
# Retired: `exec /usr/libexec/lxqt-policykit-agent`.
# That agent is broken anyway: it dies at exec on a libQt6Xdg symbol, and noctalia is the agent now.
# Why it lives in /etc, and how to undo it: see build_files/18-noctalia-shell.sh.
