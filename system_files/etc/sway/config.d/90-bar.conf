# Deliberately empty: same basename means this replaces /usr/share/sway/config.d/90-bar.conf.
# Retired: `bar { swaybar_command waybar }`.
# waybar cannot be uninstalled, so this file is all that stops two bars drawing at once.
# Why it lives in /etc, and how to undo it: see build_files/18-noctalia-shell.sh.
