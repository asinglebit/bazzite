# Deliberately empty: same basename means this replaces /usr/share/sway/config.d/60-bindings-brightness.conf.
# Retired: the brightness key bindings, driven by brightnessctl.
# Rebinding these keys from 40-bindings.conf would not work, because that file is read first.
