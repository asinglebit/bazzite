# Deliberately empty: same basename means this replaces /usr/share/sway/config.d/90-swayidle.conf.
# Retired: `swayidle -w` driving swaylock.
# noctalia idles and locks from one process, so deleting this file is the way back to swaylock.
# Why it lives in /etc, and how to undo it: see build_files/18-noctalia-shell.sh.
