# Deliberately empty: same basename means this replaces /usr/share/sway/config.d/60-bindings-volume.conf.
# Retired: the volume key bindings, driven by pactl.
# Rebinding these keys from 40-bindings.conf would not work, because that file is read first.
