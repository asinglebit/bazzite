# Deliberately empty: same basename means this replaces /usr/share/sway/config.d/60-bindings-screenshot.conf.
# Retired: the screenshot key bindings, driven by grimshot.
# Rebinding these keys from 40-bindings.conf would not work, because that file is read first.
# Why it lives in /etc, and how to undo it: see build_files/18-noctalia-shell.sh.
